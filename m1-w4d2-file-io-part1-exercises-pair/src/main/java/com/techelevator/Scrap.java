package com.techelevator;

public class Scrap {

	
	
	
	while ((line = br.readLine()) != null) {
	
	
	
	
	int sentenceCount = 0;
	int lastIndex = 0; 
	{
	for(int i=0;i<AliceWonderland.length();i++){  
	    for(int j=0;j<SENTENCE_DELIMETERS.length();j++){  
	        if(myFile.charAt(i)==SENTENCE_DELIMETERS.charAt(j)){
	            if(lastIndex!=i-1){
	                sentenceCount++;
	            }
	            lastIndex=i;
	        }
	    }
	}
}

	
	int count = str.split("[!?.:]+").length;
	
	
    String violet;
  while(Compton.ready())
  {
      violet=Compton.readLine();
      sentenceCount=violet.split("[!?.:]+").length;
      System.out.println("the number of words in line is " + 

        sentenceCount);
   }
  
  String[] words = line.split("\\.")
		  
		  
		  String[] words = line.split(Pattern.quote("."));